services:
  vnstat-ui:
    container_name: vnstat-ui
    build: .
    volumes:
      - vnstat-data:/var/lib/vnstat
    image: bhoopendrau004/vnstat-ui:latest
    restart: always
    network_mode: 'host'

volumes:
  vnstat-data: